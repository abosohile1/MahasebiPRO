# محاسبي PRO V6 — حزمة بناء APK من الهاتف

هذه الحزمة هي مشروع Android جاهز للاستيراد في بيئة بناء Android على الهاتف مثل AndroidIDE.

## الطريقة الموصى بها: AndroidIDE

1. نزّل وثبّت AndroidIDE على الهاتف.
2. فك ضغط هذا الملف.
3. افتح AndroidIDE واختر **Open Project**.
4. اختر المجلد:
   `android/`
5. انتظر مزامنة Gradle وتنزيل مكونات Android المطلوبة.
6. اختر:
   `app > Tasks > build > assembleDebug`
7. بعد نجاح البناء ستجد APK داخل:
   `android/app/build/outputs/apk/debug/app-debug.apk`
8. ثبّت APK على الهاتف الذي سيكون **الخادم الرئيسي**.

## ملاحظات

- المشروع يستخدم Android Gradle Plugin 8.7.3.
- استخدم JDK 17 أو البيئة المضمنة في AndroidIDE.
- التطبيق يتطلب Android 8.0 (API 26) أو أحدث.
- عند تشغيل الخادم تظهر خدمة Foreground وتستمع على المنفذ 8787.
- للوصول من خارج الشبكة نستخدم Tailscale، وليس فتح قاعدة البيانات مباشرة على الإنترنت.

## اختبار الخادم بعد التثبيت

1. افتح محاسبي PRO.
2. اضغط **تشغيل الخادم**.
3. يجب أن تظهر حالة: **الخادم يعمل — المنفذ 8787**.
4. من جهاز العميل اختبر الاتصال بعنوان Tailscale للهاتف الرئيسي والمنفذ 8787.

## تنبيه أمني

الإصدار الحالي من ServerService يستخدم HTTP داخل شبكة Tailscale الخاصة. لا تفتح المنفذ 8787 مباشرة على الإنترنت. يجب أن يبقى الوصول محصورًا داخل شبكة Tailscale أو نضيف HTTPS/TLS داخل التطبيق قبل تعريض الخادم لشبكة عامة.
